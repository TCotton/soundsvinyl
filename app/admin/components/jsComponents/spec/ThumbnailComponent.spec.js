/*
import { mount } from '@vue/test-utils'
import '../ThumbnailComponent.js';

xdescribe('Component thumbnail', () => {

	let component;

	beforeEach(() => {
		component = thumbnail;
	});

	it('should be defined', () => {
		expect(component).toBeDefined();
	});
});
*/

/**
 * TODO: work out how to test globally registered Vue components
 */
